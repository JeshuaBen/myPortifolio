import Header from "../../components/Header";
import * as S from "./styles";

const Home = () => {
  return (
    <S.Container>
      <Header isSelected={false} />
    </S.Container>
  );
};

export default Home;

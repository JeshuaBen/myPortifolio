import Header from "../../components/Header";
import Layout from "../../components/Layout";

const Home = () => {
  return (
    <Layout>
      <Header isSelected={false} />
    </Layout>
  );
};

export default Home;
